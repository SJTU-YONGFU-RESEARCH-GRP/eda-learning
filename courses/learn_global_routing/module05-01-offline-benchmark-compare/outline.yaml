title: Why compare
footer: learn_global_routing — 01 offline benchmark compare
slides:
- type: title
  title: Why compare
  subtitle: 'Toy global routers need a harness: same JSON in, edge overflow and runtime
    out'
  notes: 'Toy global routers need a harness: same JSON in, edge overflow and runtime
    out. Comparing L, maze, and rip-up teaches what better means beyond a pretty heat
    map.'
- type: bullets
  title: The idea
  bullets:
  - Fix tiny_gr
  - Run sequential L-HV, maze mode, and one rip-up pass
  - Record total overflow, max overflow, wall time
  - Optionally compare to a reference router
  - Write one discrepancy hypothesis
  notes: Fix tiny_gr. Run sequential L-HV, maze mode, and one rip-up pass. Record
    total overflow, max overflow, wall time. Optionally compare to a reference router.
    Write one discrepancy hypothesis.
- type: bullets
  title: Harness shape
  bullets:
  - A small Python driver loading tiny_gr.json is enough
  - Print a markdown table
  - Missing external tools are OK, document blockers and still validate I/O
  notes: A small Python driver loading tiny_gr.json is enough. Print a markdown table.
    Missing external tools are OK—document blockers and still validate I/O.
- type: bullets
  title: Pitfalls
  bullets:
  - Changing edge_capacity between rows
  - Comparing incompatible route modes without labeling
  - Optimizing runtime before correctness
  notes: Changing edge_capacity between rows. Comparing incompatible route modes without
    labeling. Optimizing runtime before correctness.
- type: bullets
  title: Your turn
  bullets:
  - Fill one comparison table for L versus maze versus rip-up
  - Then open the wrap
  notes: Fill one comparison table for L versus maze versus rip-up. Then open the
    wrap.
